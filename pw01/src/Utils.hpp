#pragma once

/**
 * @brief Utility to compute the floor modulus of a number
 * @param num The number
 * @param mod The modulus
 * @return The floor modulus of the number
 */
unsigned floorMod(long num, unsigned mod);
